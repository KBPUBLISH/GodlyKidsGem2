# Copy to .env.local. Include the /api suffix — without it, login 404s with "Endpoint not found".
# Default in constants.ts is http://localhost:5001/api/ when unset.
VITE_API_BASE_URL=http://127.0.0.1:5001/api/
