import React from 'react';

interface ProBadgeProps {
  className?: string;
}

/** Reverse trial removed - badge no longer shown. Kept for API compatibility. */
const ProBadge: React.FC<ProBadgeProps> = () => {
  return null;
};

export default ProBadge;
